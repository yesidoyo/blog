{% extends 'base.html.twig' %}

{% block title %}Noticia{% endblock %}

	{% block contenido %}
		<a id="atras" class="btn btn-primary" href="{{ url_atras }}">ATRÁS</a>	
		<h1>
			{{ noticia.id }} {{ noticia.titulo }}
		</h1>				
		<img class="imagen" src="{{asset( noticia.imagen )}}"  alt="{{ noticia.titulo }}">
		<p>{{ noticia.descripcion }}</p>
		<p>{{ noticia.fecha|date("d/m/Y") }}</p>	
	{% endblock %}

